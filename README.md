# Image Classifier (Python + Keras)
Binary classifier on CIFAR-10 subset (cats vs. dogs). Trains a tiny CNN and prints test accuracy.

## Run
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python train.py
